pm0 = read.csv("Desktop/annual_all_2012.csv")
# class()
# str()
# summary()
# missing value: are missing value I need to worry about?
# mean(is.na(x0))
# dates = as.Date(as.character(dates),"%Y%m%d)
# subset(pm1,State.Code==36,c(Country.Code,Site.ID))
# tapply: with(pm1,tapply(Sample.Value,State.Code,mean,na.rm=T))